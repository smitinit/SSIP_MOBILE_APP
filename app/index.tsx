import WelcomeScreen from "../src/screens/WelcomeScreen";

// Launch screen: background image, app text, "Get Started" -> goes to auth.
// Bottom tabs are not mounted until after authentication.
export default function App() {
  return <WelcomeScreen />;
}
